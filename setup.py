from setuptools import setup


setup(name='braininversion',
      version='0.1',
      packages=['braininversion'],
      install_requires=[
          # TODO, 
      ],
      include_package_data=True,
      zip_safe=False)